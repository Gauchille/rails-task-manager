<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %>  <%= link_to "delete", task_path(task), data: {turbo_method: :delete, turbo_confirm: "Do you really wanna delete it ?" }%></li>
  <% end %>
</ul>
<%= link_to "create a new task", new_task_path %>
